# LedgerFlow

LedgerFlow is a portfolio-quality digital wallet MVP built with Next.js and
TypeScript.

## Development

```bash
pnpm install
pnpm dev
```

Open [http://localhost:3000](http://localhost:3000).

## Production build

```bash
pnpm build
pnpm start
```
